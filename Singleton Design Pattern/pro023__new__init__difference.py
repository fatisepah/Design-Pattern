class A:
    def __new__(cls):
        return 20
    
a = A()
print(type(a))

print(a)

#وقتی میخوایم ایجاد hnstance جدید رو کنترل کنیم از __NEW__ استفاده میکنیم
#اولین قدم در ساخت instance جدید و مسئول برگرداندن instance جدید می باشد
#new قبل از init  می آید
#init برای کنترل مقداردهی اولیه استفاده می شود و چیزی را برنمی گرداند
#new و call متدهایی هستند که ما روی کلاس استفاده می کنیم و cls می گیرند
#اما init یک object متد است و self رو به عنوان پارامتر می گیرد